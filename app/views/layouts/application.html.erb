<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "codeprettythings" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="<%= asset_path("diamond-group-svgrepo-com.svg") %>" type="image/svg+xml">
    <link rel="apple-touch-icon" href="<%= asset_path("diamond-group-svgrepo-com.svg") %>">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-sand-50 text-ink-900">
    <header class="border-b border-sand-200 bg-white/80 backdrop-blur">
      <div class="container mx-auto flex items-center justify-between px-5 py-4">
        <%= link_to root_path, class: "flex items-center gap-2 text-lg font-semibold tracking-tight text-ink-900" do %>
          <%= image_tag "diamond-group-svgrepo-com.svg",
            alt: "codeprettythings",
            class: "h-7 w-7" %>
          <span class="lowercase">codeprettythings</span>
        <% end %>

        <div class="flex items-center gap-3">
          <% if signed_in? %>
            <span class="text-sm text-ink-600">Signed in</span>
            <%= button_to "Sign out",
              session_path,
              method: :delete,
              form_class: "contents",
              class: "rounded-xl bg-sand-100 px-4 py-2 text-sm font-medium text-ink-900 ring-1 ring-sand-200 hover:bg-sand-200" %>
          <% end %>
        </div>
      </div>
    </header>

    <main class="container mx-auto mt-16 px-5">
      <% flash.each do |type, message| %>
        <div
          class="<%= ["mb-4 rounded-xl border px-4 py-3 text-sm", type.to_sym == :alert ? "border-rosewood-200 bg-rosewood-50 text-rosewood-800" : "border-ink-200 bg-white text-ink-800"].join(" ") %>">
          <%= message %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
